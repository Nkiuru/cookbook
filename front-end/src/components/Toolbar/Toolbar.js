import React from 'react';
import styles from './Toolbar.module.scss';

const Toolbar = () => {
  return (
    <div>
      <h2 style={{ textAlign: 'center' }}>Toolbar</h2>
    </div>
  );
};

export default Toolbar;
