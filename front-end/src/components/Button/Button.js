import React from 'react';
import styles from './Button.module.scss';

const Button = () => {
  return (
    <div>
      <h2 style={{ textAlign: 'center' }}>Button component</h2>
    </div>
  );
};

export default Button;
