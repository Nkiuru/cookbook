import React from 'react';
import styles from './Checkbox.module.scss';

const Checkbox = () => {
  return (
    <div>
      <h2 style={{ textAlign: 'center' }}>Checkbox component</h2>
    </div>
  );
};

export default Checkbox;
